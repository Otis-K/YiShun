# tinytool-biz

该目录汇总 TinyTool / Tool Plus 的三个独立部分：

- `frontend-sdk/`：FlowCanvas 前端画布 SDK。
- `tool-plus/`：Electron 桌面工具主项目。
- `backend-sdk/`：FlowCanvas Go 后端 SDK 与模型执行层。

本目录用于统一开发和后续提交 GitHub。依赖、构建产物、安装包、本地运行数据及密钥配置未复制；请分别进入子项目安装依赖并按各自 `README.md` 构建。
