# 0.4.0 实现与验收报告

日期：2026-07-12

## 交付范围

- 完整文件分类 9 种规则。
- WebP、AVIF、TGA、PSD、SVG 图片输出，以及 9 种图片效果。
- 视频/音频的提取、截取、裁剪、合并、分辨率、帧率、码率、音量和封面视频操作。
- PDF 安全/栅格压缩、原始图片提取、页码与元数据。
- DOCX、XLSX、PPTX 内嵌图片导出、删除和指定/全部替换。
- 前端中文卡片、参数表单、默认工作区、结果完整路径和“打开保存位置”。

## 自动与压力验收

- `scripts/verify.ps1`：全部通过；后端与前端均为 114 个工具键，无缺失或多余入口。
- `STRESS_REPORT.json`：60 项通过、0 项失败。包含 9 种分类各 10,000 个输出、12 个图片效果案例、Office/PDF/音视频重压和 7 个预期失败边界。
- `DEEP_VERIFICATION.json`：58 项通过、0 项失败。独立检查文件签名、透明通道、OOXML 关系目标、Excel 4 个 drawing/20 个图片锚点、PPT 矢量媒体替换、PDF 80 页/页码/元数据和媒体流。
- Go：`go test ./...` 通过。

机器报告与样本位于 `G:\tool-user-file\stress-0.4.0`，不进入安装包。

## 原生程序与视觉验收

- Word：源/清图/替换文件均由 Word 原生打开；段落均为 85。源和替换为 10 页，清图后为 4 页。Word 的 PDF 自动导出在本机曾超时，因此没有声称 Word 视觉渲染通过；使用原生打开、包关系和媒体哈希作为验收证据。
- Excel：源/清图/替换文件均由 Excel 原生打开，均为 4 个工作表、20,004 个已用行；Shapes 数为 20/0/20。原生导出 PDF 可见源噪声图被删除，并由不同的彩色 JPEG 替换。
- PowerPoint：源/清图/替换文件均由 PowerPoint 原生打开并导出 87 张 PNG 和 PDF。清图会把幻灯片图片关系重定向到一个透明占位图，以保留形状 ID 和动画引用；原始图片负载已删除，母版/主题图片保留。PNG、JPEG、WMF、EMF 替换均可见。
- PDF：pdfcpu 与 pypdf 均验证 80 页输出可读；首末页页码文本和 Unicode 元数据精确匹配。强力栅格压缩若输出反而变大，会自动回退到安全压缩。

## 已知语义边界

- PDF 均衡/强力压缩会栅格化页面，不保留可搜索文本、链接和书签；前端已明确提示。安全模式保留文档结构。
- SVG 输出是内嵌位图的标准 SVG 容器，不宣称位图矢量化。
- Office 图片处理限定现代 OOXML：DOCX、XLSX、PPTX。
- 网页视频下载不绕过 DRM、登录、付费墙或访问控制。

## 安装包验收

- 路径：`安装包测试\0.4.0-干净安装包\文档批量处理工具 Setup 0.4.0.exe`。
- 安装包 188.43 MiB；SHA-256：`286A243E56ECF31EBEE95C48A6BE3CD9ABBCF4404E37327D8293364745ACD7AB`。
- `app.asar` 11 个条目、安装资源 520 个文件；扫描未发现 stress、测试样本、`node_modules`、`.tools`、`work` 或 `tool-user-file`。
- 真实 NSIS 安装退出码 0、耗时 50.9 秒；安装后 542 个文件、505.79 MiB。安装版 catalog=114，透明图片效果输出成功，Electron 正常保持运行，测试安装随后退出码 0 卸载干净。
- NSIS 最大压缩会先在系统临时目录展开，系统盘空间不足会导致退出码 2 或旧版卸载错误。建议安装前确保系统临时目录所在盘至少有 1 GiB 可用空间；这与安装目标盘空间是两回事。
